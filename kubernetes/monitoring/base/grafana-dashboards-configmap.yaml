apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: grafana
  name: grafana-dashboards
data:
  dashboards.yaml: |
    {
        "apiVersion": 1,
        "providers": [
            {
                "folder": "",
                "name": "0",
                "options": {
                    "path": "/grafana-dashboard-definitions/0"
                },
                "orgId": 1,
                "type": "file"
            }
        ]
    }
